---
phase: "03"
plan: "02"
name: "Tax Intelligence Page"
wave: 2
depends_on:
  - "03-01"
files_modified:
  - app/(dashboard)/families/[familyId]/tax/page.tsx
  - components/tax/capital-gains-summary.tsx
  - components/tax/compliance-vault.tsx
  - components/tax/fy-toggle.tsx
autonomous: true
---

<objective>
Create the Tax Intelligence page at `/families/[familyId]/tax` following the design in `tax_and_ai.html`. This page shows capital gains summary, FY toggle, and Compliance Vault exemption tracker.

Purpose: Users can see their tax liability breakdown per holding for the selected FY.
Output: Tax Intelligence page with capital gains summary cards and FY selection.
</objective>

<context>
@.planning/phases/03-tax-engine/03-CONTEXT.md
@.planning/phases/03-tax-engine/03-RESEARCH.md
@.planning/UI-design/tax_and_ai.html

**UI Design from tax_and_ai.html:**
- Capital gains FY summary card (8/12 cols) + Compliance Vault (4/12 cols) at top
- FY toggle in sticky header (current FY / prior FY)
- Full-width dark harvesting hero below
- Follows MD3 tokens, Manrope headings, Material Symbols icons
</context>

<tasks>

<task type="auto">
<name>Create Tax Intelligence page route</name>
<action>
1. Create `app/(dashboard)/families/[familyId]/tax/page.tsx` as Server Component:
   - Fetch all holders for the family
   - For each holder, fetch transactions via get_holder_analytics_transactions (no date filter for FIFO)
   - Fetch current NAVs for all schemes
   - Fetch grandfathering_nav data
   - Call lib/tax/engine computeTaxSummary for each holder
   - Aggregate LTCG/STCG totals

2. Implement FY toggle:
   - URL param: ?fy=current (default) or ?fy=prior
   - Pass fyBounds to tax engine
   - When prior FY selected: show realized gains for that FY, harvesting becomes read-only

3. Layout: 8/12 col capital gains summary + 4/12 col Compliance Vault
</action>
<verify>
- npx tsc --noEmit passes
- Page loads without errors on dev server
- FY toggle switches data correctly
</verify>
<done>Tax Intelligence page created with server-side data fetching</done>
</task>

<task type="auto">
<name>Create Capital Gains Summary card</name>
<action>
1. Create `components/tax/capital-gains-summary.tsx`:
   - Shows LTCG total, STCG total, total tax liability
   - Breakdown per holding in table format
   - Uses bento card pattern: bg-surface-container-lowest rounded-2xl shadow-sm
   - Format: ₹ with tabular-nums class

2. Display:
   - Total Realized LTCG (this FY)
   - Total Realized STCG (this FY)
   - Exemption used: ₹X of ₹1,25,000
   - Estimated tax liability
</action>
<verify>
- Component renders correctly with tax data
- INR formatting with ₹ symbol and commas
- Responsive layout (8 cols)
</verify>
<done>Capital gains summary card component created</done>
</task>

<task type="auto">
<name>Create Compliance Vault component</name>
<action>
1. Create `components/tax/compliance-vault.tsx` (replaces ITR download):
   - LTCG exemption tracker: ₹X of ₹1,25,000 used (progress bar)
   - Remaining exemption available: ₹Y
   - Key deadline: March 31 (days remaining badge)
   - FY-end tax liability estimate

2. Use Badge component for days-to-March-31 countdown
3. Use Progress bar for exemption usage
</action>
<verify>
- Component displays exemption progress correctly
- Days countdown calculates correctly
- 4-column responsive width
</verify>
<done>Compliance Vault exemption tracker created</done>
</task>

<task type="auto">
<name>Create FY toggle component</name>
<action>
1. Create `components/tax/fy-toggle.tsx`:
   - 2-option toggle: "This FY" / "Prior FY"
   - Uses URL search params (fy=current | fy=prior)
   - Sync with page URL on toggle

2. When prior FY selected:
   - All data reflects that FY
   - Harvesting section shows read-only historical data
   - Compliance Vault reflects selected FY
</action>
<verify>
- Toggle updates URL and page data
- Prior FY shows correct historical data
- Accessible: keyboard navigation works
</verify>
<done>FY toggle component created</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit passes
- Page renders at /families/[familyId]/tax
- FY toggle switches between current and prior FY
- Capital gains and compliance vault display correctly
</verification>

<success_criteria>
- [ ] Tax Intelligence page created at /families/[familyId]/tax
- [ ] Capital gains summary shows LTCG/STCG breakdown per holder
- [ ] FY toggle switches between current and prior FY
- [ ] Compliance Vault shows exemption tracker
- [ ] All UI follows tax_and_ai.html design (MD3 tokens, Manrope, Material Symbols)
- [ ] Server-side data fetching with RPC calls
- [ ] No LLM involvement - rule-based calculations only
</success_criteria>

<must_haves>
- User can see LTCG and STCG breakdown per holding (TAX-01)
- User can switch between current FY and prior FY (TAX-01)
- Compliance Vault shows exemption usage and deadline
- Page loads without errors
</must_haves>

<dependencies>
- Depends on: 03-01 (tax engine library must exist first)
- Reads: transactions table, grandfathering_nav table
- Reuses: Dialog, Badge components from existing UI library
</dependencies>

<commit>
feat(03-02): create Tax Intelligence page UI

- app/(dashboard)/families/[familyId]/tax/page.tsx - Tax page server component
- components/tax/capital-gains-summary.tsx - LTCG/STCG breakdown cards
- components/tax/compliance-vault.tsx - Exemption tracker
- components/tax/fy-toggle.tsx - FY selection toggle
</commit>
