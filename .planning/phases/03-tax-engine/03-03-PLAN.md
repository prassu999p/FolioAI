---
phase: "03"
plan: "03"
name: "Sell Tax Estimator Modal"
wave: 2
depends_on:
  - "03-01"
files_modified:
  - components/holdings/holdings-table.tsx
  - components/tax/sell-tax-estimator-modal.tsx
autonomous: true
---

<objective>
Add "Estimate Tax" action to the holdings table and create a modal dialog that shows real-time tax estimation when the user enters units to sell.

Purpose: Users can see "If I sell X units today, my estimated tax is ₹Y" without LLM involvement.
Output: Modal dialog with units input and tax breakdown.
</objective>

<context>
@.planning/phases/03-tax-engine/03-CONTEXT.md
@.planning/phases/03-tax-engine/03-RESEARCH.md

**Key requirements from context:**
- Lives inline on holdings table as "Estimate Tax" action per row
- Clicking opens a modal dialog (not inline row expansion)
- Modal shows: fund name, units input, LTCG gain, STCG gain, applicable rates, total estimated tax
- Grandfathering applied automatically
- Calculation is real-time, rule-based - no LLM
- No estimator section on Tax Intelligence page itself - lives on holder's holdings table
</context>

<tasks>

<task type="auto">
<name>Add Estimate Tax action to HoldingsTable</name>
<action>
1. Modify `components/holdings/holdings-table.tsx`:
   - Add "Estimate Tax" action button to each row
   - Uses existing actions column or creates new one
   - Opens SellTaxEstimatorModal with holding data

2. Pass to modal:
   - folio_id, scheme_name, current units, current value
   - Tax lots for this holding (pre-computed)
   - Current NAV, grandfathering NAV
</action>
<verify>
- HoldingsTable still renders correctly
- "Estimate Tax" button appears in each row
- Clicking opens modal with correct holding data
</verify>
<done>Holdings table extended with Estimate Tax action</done>
</task>

<task type="auto">
<name>Create Sell Tax Estimator Modal</name>
<action>
1. Create `components/tax/sell-tax-estimator-modal.tsx`:
   - 'use client' directive
   - Uncontrolled Dialog pattern (no open/onOpenChange - matches SetTargetModal)
   - DialogTrigger wraps "Estimate Tax" button

2. Modal content:
   - Fund name display
   - Units input field (number, step 0.001)
   - Real-time calculation as user types
   - Breakdown:
     - LTCG gain (₹)
     - STCG gain (₹)
     - Applicable tax rate per type
     - Total estimated tax liability
   - Grandfathering applied automatically if holding qualifies
   - "Reinvest in same fund" hint

3. Computation:
   - Uses lib/tax/engine functions client-side
   - No API call - pure computation
</action>
<verify>
- Modal opens with correct holding data
- Units input updates calculation in real-time
- Tax breakdown shows correct LTCG/STCG split
- Grandfathering applied correctly for pre-2018 holdings
- npx tsc --noEmit passes
</verify>
<done>Sell Tax Estimator Modal created</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit passes
- Modal opens from holdings table
- Tax estimation calculates correctly
- Responsive and accessible
</verification>

<success_criteria>
- [ ] "Estimate Tax" action added to each holdings row
- [ ] Modal opens with correct holding data
- [ ] Units input shows real-time tax estimation
- [ ] LTCG/STCG breakdown displayed
- [ ] Grandfathering applied automatically
- [ ] No API calls - pure client-side computation
- [ ] Uses uncontrolled Dialog pattern (like SetTargetModal)
</success_criteria>

<must_haves>
- User can see "If I sell X units, my tax is ₹Y" (TAX-03)
- Grandfathering applied correctly for pre-Feb 2018 holdings (TAX-02)
- Real-time calculation without page reload
- No LLM involvement - rule-based
</must_haves>

<dependencies>
- Depends on: 03-01 (tax engine library)
- Reuses: components/ui/dialog.tsx (existing), holdings-table.tsx (existing)
</dependencies>

<commit>
feat(03-03): add sell tax estimator modal

- components/holdings/holdings-table.tsx - added Estimate Tax action
- components/tax/sell-tax-estimator-modal.tsx - modal with real-time calculation
</commit>
