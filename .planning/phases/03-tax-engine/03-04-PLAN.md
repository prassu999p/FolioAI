---
phase: "03"
plan: "04"
name: "LTCG Harvesting Hero Section"
wave: 2
depends_on:
  - "03-01"
files_modified:
  - app/(dashboard)/families/[familyId]/tax/page.tsx
  - components/tax/harvesting-hero.tsx
autonomous: true
---

<objective>
Create the LTCG harvesting suggestions hero section with dark bg-primary styling from the design. Shows actionable suggestions for consuming the ₹1.25L annual exemption.

Purpose: Users receive specific recommendations on which funds to sell to minimize tax.
Output: Full-width dark hero card with harvesting suggestions list.
</objective>

<context>
@.planning/phases/03-tax-engine/03-CONTEXT.md
@.planning/phases/03-tax-engine/03-RESEARCH.md
@.planning/UI-design/tax_and_ai.html

**Key requirements from context:**
- Dark hero card: bg-primary text-on-primary rounded-3xl
- Scope: all eligible holdings with unrealized LTCG > ₹0
- Engine picks optimal set (sorted by LTCG descending until exemption consumed)
- Display-only - no "mark as harvested" state tracking
- Each suggestion row shows:
  - Fund name
  - Units to sell
  - LTCG to book (₹)
  - Remaining exemption consumed
  - Estimated tax saved
  - "Reinvest proceeds in same fund" instruction
- When prior FY selected: show historical data only (read-only)
</context>

<tasks>

<task type="auto">
<name>Create Harvesting Hero component</name>
<action>
1. Create `components/tax/harvesting-hero.tsx`:
   - Full-width dark card: bg-primary text-on-primary rounded-3xl
   - Header: "Harvest ₹1,25,000 in LTCG now to save ₹X in taxes"
   - Inner "Execution Plan" card with suggestions list

2. Display suggestions:
   - Fund name
   - Units to sell (to 3 decimal places)
   - LTCG to book (₹)
   - Exemption consumed so far
   - Tax saved calculation
   - "Reinvest proceeds in the same fund" on each row

3. Use lib/tax/harvesting.ts computeHarvestingSuggestions

4. When prior FY selected: show read-only historical harvesting data (no action)
</action>
<verify>
- Hero renders with dark bg-primary styling
- Suggestions list displays correctly
- Tax saved number prominent in header
- Read-only mode when prior FY selected
</verify>
<done>Harvesting hero component created</done>
</task>

<task type="auto">
<name>Integrate harvesting into Tax Intelligence page</name>
<action>
1. Update `app/(dashboard)/families/[familyId]/tax/page.tsx`:
   - Import harvesting-hero component
   - Pass unrealized gains data from tax engine
   - Pass ltcgUsedThisFY for exemption calculation
   
2. Position: Below the capital gains summary and Compliance Vault (full-width)

3. Handle prior FY mode:
   - If fy=prior: show historical harvesting data (realized LTCG from that FY)
   - If fy=current: show actionable suggestions with units to sell
</action>
<verify>
- Page renders with harvesting hero below summary cards
- Suggestions calculate correctly based on unrealized gains
- FY toggle affects whether suggestions are actionable
</verify>
<done>Harvesting hero integrated into Tax page</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit passes
- Hero displays at bottom of Tax Intelligence page
- Suggestions show optimal units to sell
- Tax savings prominently displayed
- Read-only when viewing prior FY
</verification>

<success_criteria>
- [ ] Dark hero card with bg-primary styling
- [ ] Shows optimal harvesting suggestions
- [ ] Each suggestion shows fund, units, LTCG, tax saved
- [ ] "Reinvest proceeds" instruction on each row
- [ ] Read-only mode for prior FY
- [ ] Follows tax_and_ai.html design
</success_criteria>

<must_haves>
- User receives LTCG harvesting suggestions (TAX-04)
- Suggestions show exactly how many units to sell per fund
- Tax saved calculation prominent
- Forward-looking (current FY) vs historical (prior FY) handled
</must_haves>

<dependencies>
- Depends on: 03-01 (harvesting algorithm), 03-02 (Tax page layout)
- Uses: lib/tax/harvesting.ts computeHarvestingSuggestions
</dependencies>

<commit>
feat(03-04): add LTCG harvesting hero section

- components/tax/harvesting-hero.tsx - dark card with suggestions
- Updated tax page to integrate harvesting section
</commit>
