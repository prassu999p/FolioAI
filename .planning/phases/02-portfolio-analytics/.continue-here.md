---
phase: 02-portfolio-analytics
task: css-visual-fix
total_tasks: 1
status: in_progress
last_updated: 2026-03-20T10:15:41.713Z
---

<current_state>
Phase 02 gap closure (plan 02-15) executed and verified (10/10 requirements, status: human_needed). Now mid-fix of CSS/styling issues so the live app matches Family_view.html. postcss.config.mjs was missing (root cause — Tailwind v4 was never compiled at all). Fixes applied but browser confirmation pending.
</current_state>

<completed_work>

- Plan 02-15: Full family dashboard implementation — DONE (commits 753dad7, baa9bf1)
- Verification: 10/10 requirements verified (status: human_needed, 7 browser tests pending)
- Root cause diagnosed: `postcss.config.mjs` was missing — Tailwind v4 never compiled
- `postcss.config.mjs` created with `@tailwindcss/postcss` plugin
- `app/(dashboard)/layout.tsx` rebuilt: all 6 nav items in design order, active state (bg-white rounded-r-full shadow-sm), Ask AI CTA button, Support/Settings links
- `app/(dashboard)/families/[familyId]/page.tsx` updated: sticky TopAppBar with "Family Dashboard" title, Import CAS link, notification/settings icon buttons, avatar initial
- `family-dashboard.tsx`: removed duplicate header (family name + Add Holder/Sync buttons) and unused SyncButton/AddHolderForm imports
- `app/globals.css` fixed:
  - Removed `--color-background: var(--background)` indirect mapping (was pointing at shadcn white `oklch(1 0 0)`)
  - Added `--color-border: #c4c6d0`, `--color-ring: #c4c6d0`, `--color-foreground: #001f2a` to `@theme inline`
  - Fixed `@apply border-border outline-ring/50` → `@apply border-border` (opacity modifier on non-alpha token causes parse error)
  - Fixed `@apply bg-background text-foreground` body → direct `background-color: #f4faff; color: #001f2a`
</completed_work>

<remaining_work>

- Confirm dev server compiles without errors (user was seeing `Cannot apply unknown utility class border-border`)
- Restart dev server to pick up postcss.config.mjs (`npm run dev`)
- Browser-verify the 7 human verification items from 02-VERIFICATION.md:
  1. Period toggle updates all metrics
  2. View-mode tabs highlight correct cards
  3. Nifty 50 benchmark renders in XIRR card
  4. SIP XIRR appears per row
  5. This FY button scopes to April 1 – March 31
  6. SetTargetModal opens after Plan 09 fix
  7. Allocation bar deviation coloring
- Playwright MCP was being set up (`claude mcp add playwright npx @playwright/mcp@latest`) — needs Claude Code restart to load
- Commit the CSS/layout fixes once confirmed working
</remaining_work>

<decisions_made>

- Used inline `style={{}}` props for MD3 color tokens in layout.tsx/page.tsx sidebar — guarantees correct colors even if Tailwind token resolution has any edge cases
- Removed shadcn `--color-background: var(--background)` indirect mapping — MD3 value `#f4faff` now set directly in `@theme inline`
- Added `--color-border: #c4c6d0` (same as `outline-variant`) to satisfy `@apply border-border` without changing visual appearance
- TopAppBar lives in page.tsx (not layout.tsx) — allows per-page title/action customization
- `family-dashboard.tsx` no longer imports SyncButton/AddHolderForm — those controls moved to page-level header
</decisions_made>

<blockers>

- Dev server must be restarted to pick up new `postcss.config.mjs` (Next.js caches PostCSS config at startup)
- Playwright MCP needs Claude Code restart after `claude mcp add` command
</blockers>

<context>
Phase 02 is functionally complete — all 10 requirements verified in code. The CSS was broken because postcss.config.mjs never existed (shadcn/Next.js scaffold omitted it), so zero Tailwind utilities compiled. After postcss.config.mjs + globals.css fixes, the app should render the full MD3 design matching Family_view.html.

The 7 browser verification items require real data (imported CAS with SIP transactions, seeded nifty50_daily) plus live interaction. Playwright MCP would automate most checks once loaded.
</context>

<next_action>
Start with: Restart dev server (`npm run dev`), hard-refresh browser at /families/[familyId], confirm layout matches Family_view.html. Then restart Claude Code to load Playwright MCP for the 7 browser checks.
</next_action>
