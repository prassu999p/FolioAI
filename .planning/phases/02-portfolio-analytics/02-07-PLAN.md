---
phase: 02-portfolio-analytics
plan: "07"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - app/(dashboard)/layout.tsx
  - components/family/family-dashboard.tsx
autonomous: true
gap_closure: true
requirements: [PERF-01, PERF-02, PERF-03, PERF-04, PERF-05, PERF-06, SIP-01, SIP-02, ALLOC-01, ALLOC-02]

must_haves:
  truths:
    - "Pages render with #001736 primary color (deep navy), not near-black oklch"
    - "Body text uses Work Sans; headings use Manrope"
    - "Dashboard has a fixed w-64 left sidebar and ml-64 main content area matching frontend.html"
    - "Family dashboard cards use MD3 surface tokens (bg-surface-container-lowest, text-on-surface) not shadcn aliases"
  artifacts:
    - path: "app/globals.css"
      provides: "MD3 color tokens as sole source of truth in @theme inline — shadcn alias block removed"
    - path: "app/(dashboard)/layout.tsx"
      provides: "Fixed sidebar navigation + ml-64 main layout"
    - path: "components/family/family-dashboard.tsx"
      provides: "Component using MD3 token class names throughout"
  key_links:
    - from: "app/globals.css @theme inline"
      to: "Tailwind color utilities (bg-primary, text-on-surface, etc.)"
      via: "--color-primary hex value survives to Tailwind"
      pattern: "--color-primary: #001736"
    - from: "app/(dashboard)/layout.tsx"
      to: "frontend.html sidebar structure"
      via: "fixed aside w-64 + ml-64 main"
      pattern: "fixed.*w-64|ml-64"
---

<objective>
Fix the MD3 design system so all pages render with the correct color tokens, typography, and layout matching frontend.html.

Purpose: The family dashboard currently renders unstyled — near-black primary color, Geist font, top navbar layout. This is caused by 4 compounding root causes in globals.css, layout.tsx, and family-dashboard.tsx.

Output: globals.css with clean MD3-only @theme block; dashboard layout with fixed sidebar; family-dashboard with MD3 token classes.
</objective>

<execution_context>
@/Users/prasanthp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prasanthp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-portfolio-analytics/02-CONTEXT.md
@.planning/frontend.html
@app/globals.css
@app/(dashboard)/layout.tsx
@components/family/family-dashboard.tsx
</context>

<interfaces>
<!-- MD3 color tokens already defined in globals.css lines 13-59. These are CORRECT. -->
<!-- The problem is lines 86-93 in @theme inline which overwrite them with shadcn oklch aliases. -->

Correct MD3 primary (must survive to Tailwind):
  --color-primary: #001736          (deep navy — line 58 of globals.css)
  --color-on-primary: #ffffff
  --color-surface: #f4faff
  --color-on-surface: #001f2a
  --color-surface-container-lowest: #ffffff
  --color-on-surface-variant: #43474f
  --color-surface-container-low: #e6f6ff
  --color-outline-variant: #c4c6d0

Shadcn :root CSS vars (must STAY in :root, lines 100-133 of globals.css — do not touch these):
  --primary: oklch(0.205 0 0)
  --card: oklch(1 0 0)
  etc.

frontend.html sidebar structure (lines 107-160):
  <aside class="flex flex-col fixed left-0 top-0 h-screen z-40 bg-[#e6f6ff] w-64">
    <div class="p-8">FolioAI logo</div>
    <nav class="flex-1 px-4 space-y-2">nav links</nav>
    <div class="p-4 mt-auto">Ask AI button + support/settings links</div>
  </aside>
  <main class="ml-64 min-h-screen">
    <header ...sticky top-0>breadcrumb + actions</header>
    <div class="px-12 pb-12">page content</div>
  </main>
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix globals.css — remove shadcn aliases from @theme inline, fix font-sans</name>
  <files>app/globals.css</files>
  <action>
    In `app/globals.css`, make two targeted edits inside the `@theme inline { }` block:

    1. Delete lines 86-97 (the shadcn alias block). These are the lines that start with `--color-sidebar-ring: var(--sidebar-ring);` and end with `--radius-xl: calc(var(--radius) + 4px);`. They shadow the correct MD3 hex values set on lines 13-59. The :root block (lines 100-133) already defines the shadcn CSS vars — the @theme inline block does NOT need to re-alias them, because Tailwind v4 reads --color-* from @theme inline directly as hex values.

    2. On line 9, change:
       `--font-sans: var(--font-geist-sans);`
       to:
       `--font-sans: "Work Sans", sans-serif;`

    The result: @theme inline contains ONLY the MD3 color tokens (lines 13-59) and the MD3 font families (lines 61-64) plus the updated --font-sans. The :root block and .dark block remain untouched.

    Do NOT touch the :root block, the .dark block, or the @layer base block.
  </action>
  <verify>
    <automated>grep -n "var(--primary)" app/globals.css | grep "color-primary" | wc -l</automated>
    Expected: 0 (no shadcn alias overwriting --color-primary in @theme inline)
    Also verify: grep "color-primary: #001736" app/globals.css — should return 1 match
    Also verify: grep "font-sans.*Work Sans" app/globals.css — should return 1 match
  </verify>
  <done>
    `--color-primary` in @theme inline resolves to `#001736` (not oklch). `--font-sans` maps to Work Sans. Shadcn aliases are only in :root, not in @theme inline.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite dashboard layout.tsx with fixed sidebar + ml-64 main</name>
  <files>app/(dashboard)/layout.tsx</files>
  <action>
    Replace the entire content of `app/(dashboard)/layout.tsx` with the fixed sidebar layout matching frontend.html.

    The new layout must:
    - Keep the existing auth check (getClaims / redirect) — do not remove it
    - Render a fixed `<aside>` with classes: `flex flex-col fixed left-0 top-0 h-screen z-40 bg-surface-container-low w-64`
    - Inside the aside:
      - Logo section: `<div class="p-8"><h1 class="text-2xl font-black font-headline text-primary">FolioAI</h1><p class="text-xs font-medium text-on-surface-variant/70 uppercase tracking-widest mt-1">The Digital Fiduciary</p></div>`
      - Nav links section: `<nav class="flex-1 px-4 space-y-2">` with Link items for: Family Dashboard (/families), AI Insights (#), Tax Intelligence (#), Goals (#). Each link: `flex items-center px-4 py-3 text-on-surface/70 hover:text-primary hover:bg-surface-container/50 transition-all rounded-lg`. Use Material Symbols Outlined icons: dashboard, pie_chart, receipt_long, track_changes, auto_awesome.
      - Bottom section: `<div class="p-4 mt-auto">` with a "Sync NAV" button using the SyncButton import and settings/support links.
    - Render `<main class="ml-64 min-h-screen bg-surface">` wrapping `{children}` directly (no max-w-7xl wrapper — pages control their own padding)

    Import `Link` from `next/link`. Keep the `SyncButton` import. Remove the top `<nav>` bar entirely.

    The layout is a Server Component — keep `async function`. Material Symbols font is loaded via globals.css or the root layout; assume it is available.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "dashboard)/layout" | head -5</automated>
    Expected: no TypeScript errors for this file.
    Also: grep "fixed left-0" app/\(dashboard\)/layout.tsx — should return 1 match
    Also: grep "ml-64" app/\(dashboard\)/layout.tsx — should return 1 match
  </verify>
  <done>
    Layout renders a fixed w-64 sidebar and ml-64 main. No top navbar. TypeScript clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update family-dashboard.tsx to use MD3 token classes</name>
  <files>components/family/family-dashboard.tsx</files>
  <action>
    Replace all shadcn alias class names in `components/family/family-dashboard.tsx` with MD3 token equivalents:

    Replacements (class name mapping):
    - `bg-card` → `bg-surface-container-lowest`
    - `text-muted-foreground` → `text-on-surface-variant`
    - `bg-accent` → `bg-surface-container`
    - `border` (bare) → `border border-outline-variant/30`
    - `text-green-600` → `text-secondary` (MD3 green token)
    - `text-red-600` → `text-error`
    - `text-primary` (already MD3 — keep as-is)
    - `font-mono` → `tabular-nums font-body` (use Work Sans with tabular nums)
    - `text-2xl font-semibold` on the family name h1 → `text-2xl font-extrabold font-headline text-primary`
    - `text-lg font-semibold` on "Holders" h2 → `text-lg font-bold font-headline text-primary`

    The card structure for each holder should match the frontend.html bento card pattern:
    `rounded-2xl shadow-sm bg-surface-container-lowest p-6 hover:bg-surface-container transition-colors`

    The Family Total row card:
    `rounded-2xl shadow-sm bg-surface-container-lowest border-l-4 border-primary p-6`

    Keep all data logic, TypeScript types, and imports exactly as-is. Only change Tailwind class strings.
  </action>
  <verify>
    <automated>grep -n "bg-card\|text-muted-foreground\|bg-accent\b" components/family/family-dashboard.tsx | wc -l</automated>
    Expected: 0 (all shadcn aliases replaced)
    Also: npx tsc --noEmit 2>&1 | grep "family-dashboard" | head -5 — no errors
  </verify>
  <done>
    No `bg-card`, `text-muted-foreground`, or `bg-accent` remain. All cards use MD3 surface tokens. TypeScript clean.
  </done>
</task>

</tasks>

<verification>
After all 3 tasks complete, do a final sweep:

```bash
# No shadcn color aliases remain in @theme inline
grep -n "var(--primary)\|var(--card)\|var(--muted)" app/globals.css | grep -v "^[0-9]*:.*:root\|^[0-9]*:.*\.dark"

# MD3 primary is the deep navy
grep "color-primary: #001736" app/globals.css

# Sidebar layout present
grep "fixed left-0.*w-64\|w-64.*fixed" app/\(dashboard\)/layout.tsx

# No shadcn aliases in dashboard component
grep "bg-card\|text-muted-foreground" components/family/family-dashboard.tsx

# TypeScript still clean
npx tsc --noEmit 2>&1 | head -20
```
</verification>

<success_criteria>
- `app/globals.css` @theme inline block contains only MD3 hex color tokens and MD3 font declarations. No `var(--primary)` or similar shadcn aliases. `--font-sans` maps to Work Sans.
- `app/(dashboard)/layout.tsx` renders a fixed left sidebar (w-64) and ml-64 main content area. No top navbar.
- `components/family/family-dashboard.tsx` uses only MD3 token class names. No `bg-card`, `text-muted-foreground`, or `bg-accent`.
- `npx tsc --noEmit` passes with no new errors.
</success_criteria>

<output>
After completion, create `.planning/phases/02-portfolio-analytics/02-07-SUMMARY.md`
</output>
