---
phase: 02-portfolio-analytics
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/xirr.test.ts
  - tests/analytics.test.ts
  - tests/sip-detector.test.ts
  - tests/allocation.test.ts
autonomous: true
requirements: [PERF-01, PERF-02, PERF-03, PERF-05, PERF-06, SIP-01, SIP-02, ALLOC-01, ALLOC-02]

must_haves:
  truths:
    - "Four test files exist with it.todo() stubs covering all Phase 2 requirements"
    - "npm test passes with all stubs showing as pending (not failing)"
    - "Test file imports reference the correct lib/analytics/* module paths that later plans will create"
  artifacts:
    - path: "tests/xirr.test.ts"
      provides: "XIRR unit test stubs (PERF-02)"
    - path: "tests/analytics.test.ts"
      provides: "Gain/loss, benchmark, period, FY unit test stubs (PERF-01, PERF-03, PERF-05, PERF-06)"
    - path: "tests/sip-detector.test.ts"
      provides: "SIP detection and SIP XIRR stubs (SIP-01, SIP-02)"
    - path: "tests/allocation.test.ts"
      provides: "Allocation schema and asset class mapper stubs (ALLOC-01, ALLOC-02)"
  key_links:
    - from: "tests/xirr.test.ts"
      to: "lib/analytics/xirr.ts"
      via: "import { computeXIRR } from '@/lib/analytics/xirr'"
    - from: "tests/sip-detector.test.ts"
      to: "lib/analytics/sip-detector.ts"
      via: "import { detectActiveSIPs } from '@/lib/analytics/sip-detector'"
    - from: "tests/allocation.test.ts"
      to: "lib/analytics/asset-class-mapper.ts"
      via: "import { mapCategoryToAssetClass } from '@/lib/analytics/asset-class-mapper'"
---

<objective>
Create four Vitest test scaffold files with it.todo() stubs for all Phase 2 analytics requirements. This is Wave 0 — test files must exist before implementation begins so later plans have automated verify commands.

Purpose: Nyquist compliance — every implementation task needs an <automated> verify command. These stubs define the exact test IDs that implementation plans will make green.
Output: 4 test files, 0 failing tests, all stubs pending. npm test passes.
</objective>

<execution_context>
@/Users/prasanthp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prasanthp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-portfolio-analytics/02-CONTEXT.md
@.planning/phases/02-portfolio-analytics/02-RESEARCH.md
@.planning/phases/02-portfolio-analytics/02-VALIDATION.md

Existing test infrastructure from Phase 1:
- Vitest 3.0 configured via `vitest.config.mts`
- `tests/setup.ts` has TEST_HOLDER_ID, TEST_FAMILY_ID as valid UUIDs
- Pattern: `it.todo('description')` used for Wave 0 stubs (see Phase 1 Plans 01-03)
- All existing tests pass: 37 tests green
- Import aliases: `@/lib/...` resolves to project root
</context>

<feature>
  <name>Wave 0: Test Scaffolds for Phase 2 Analytics</name>
  <files>tests/xirr.test.ts, tests/analytics.test.ts, tests/sip-detector.test.ts, tests/allocation.test.ts</files>
  <behavior>
    xirr.test.ts stubs:
    - "returns correct XIRR for known cashflow series (Excel-verified)"
    - "returns null when fewer than 2 cashflows"
    - "returns null when all cashflows are negative (no positive terminal value)"
    - "returns null when NAV is missing (current_value null)"
    - "handles portfolio younger than 7 days — shows null not zero"
    - "purchase amounts must be negated (outflows) in cashflow array"

    analytics.test.ts stubs:
    - "computeGainLoss: positive gain when current_value > total_invested"
    - "computeGainLoss: negative loss when current_value < total_invested"
    - "computeGainLoss: returns null when current_value is null"
    - "getPeriodBounds: 1M returns start date 30 days ago"
    - "getPeriodBounds: all returns null (no filter)"
    - "getCurrentFY: April 1 is start of Indian financial year"
    - "getCurrentFY: March 31 is end of Indian financial year"
    - "benchmark XIRR: synthetic Nifty cashflows follow same sign convention as portfolio cashflows"

    sip-detector.test.ts stubs:
    - "detects active SIP: 3+ same-amount transactions within 30-day cadence in last 90 days"
    - "returns empty array when fewer than 3 recurring transactions"
    - "returns empty array when transactions older than 90 days"
    - "infers next debit date as lastDate + median interval (using date-fns addMonths)"
    - "SIP XIRR uses only sip transaction_type cashflows"
    - "SIP XIRR excludes purchase/lumpsum cashflows"

    allocation.test.ts stubs:
    - "AllocationTargetSchema: accepts valid allocation summing to 100"
    - "AllocationTargetSchema: rejects allocation summing to more than 100"
    - "AllocationTargetSchema: accepts partial allocation summing to less than 100"
    - "mapCategoryToAssetClass: Equity Schemes → equity"
    - "mapCategoryToAssetClass: Debt Schemes → debt"
    - "mapCategoryToAssetClass: Gold ETF → gold"
    - "mapCategoryToAssetClass: International FoF → international"
    - "mapCategoryToAssetClass: unknown category falls back to equity"
  </behavior>
  <implementation>
    Create each file with describe() block + it.todo() stubs. Import from the lib/analytics/* paths that Plan 02 will create. Use vitest's `it.todo` (no callback) so tests show as pending, not failing. Do NOT import from files that don't exist yet — just declare the import at the top as a comment or use dynamic import inside the test body that won't execute. Actually, use the standard Phase 1 pattern: import at top, tests are stubs with it.todo() so the import is never exercised. Vitest skips todo stubs without executing them so the import error won't surface until a real test runs.

    IMPORTANT: Use `import type` for types to avoid runtime errors on missing files. For function imports, wrap in the describe block comment to signal intent without causing module resolution failure at test collection time. The cleanest pattern (verified from Phase 1): declare the describe block without top-level imports from non-existent modules — only import inside beforeAll or individual tests when they transition from todo to real.

    Actually the correct Phase 1 pattern: look at existing test stubs — they DO have the imports at the top, vitest handles the case where it.todo has no callback (module is never executed). Use vi.mock() if needed or simply write the imports as comments showing what will be needed.

    Safest approach for stubs: write the describe block with import path in a comment, all stubs as it.todo('...'), no actual imports from non-existent modules. This guarantees npm test passes today.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create XIRR and analytics test scaffolds</name>
  <files>tests/xirr.test.ts, tests/analytics.test.ts</files>
  <behavior>
    - xirr.test.ts: describe('computeXIRR') with 6 it.todo() stubs
    - analytics.test.ts: describe('analytics utils') with 8 it.todo() stubs (gain/loss, period bounds, FY, benchmark)
    - Both files: comment block at top showing future imports from @/lib/analytics/xirr and @/lib/analytics/period-utils
    - No actual imports from non-existent modules — only comments
    - npm test passes (stubs are pending, not failing)
  </behavior>
  <action>
    Create tests/xirr.test.ts:
    ```typescript
    import { describe, it } from 'vitest'
    // Future import (created in Plan 02):
    // import { computeXIRR } from '@/lib/analytics/xirr'

    describe('computeXIRR', () => {
      it.todo('returns correct XIRR for known cashflow series (Excel-verified)')
      it.todo('returns null when fewer than 2 cashflows')
      it.todo('returns null when all cashflows are negative (no positive terminal value)')
      it.todo('returns null when NAV is missing (current_value null)')
      it.todo('handles portfolio younger than 7 days — shows null not zero')
      it.todo('purchase amounts must be negated (outflows) in cashflow array')
    })
    ```

    Create tests/analytics.test.ts:
    ```typescript
    import { describe, it } from 'vitest'
    // Future imports (created in Plan 02):
    // import { computeGainLoss } from '@/lib/analytics/xirr'
    // import { getPeriodBounds, getCurrentFY } from '@/lib/analytics/period-utils'
    // import { buildBenchmarkCashflows } from '@/lib/analytics/xirr'

    describe('computeGainLoss', () => {
      it.todo('positive gain when current_value > total_invested')
      it.todo('negative loss when current_value < total_invested')
      it.todo('returns null when current_value is null')
    })

    describe('getPeriodBounds', () => {
      it.todo('1M returns start date 30 days ago')
      it.todo('all returns null (no filter)')
    })

    describe('getCurrentFY', () => {
      it.todo('April 1 is start of Indian financial year')
      it.todo('March 31 is end of Indian financial year')
    })

    describe('benchmark XIRR', () => {
      it.todo('synthetic Nifty cashflows follow same sign convention as portfolio cashflows')
    })
    ```
  </action>
  <verify>
    <automated>npm test -- tests/xirr.test.ts tests/analytics.test.ts</automated>
  </verify>
  <done>Both files exist, all stubs show as "todo" (pending) in vitest output, 0 failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create SIP detector and allocation test scaffolds</name>
  <files>tests/sip-detector.test.ts, tests/allocation.test.ts</files>
  <behavior>
    - sip-detector.test.ts: describe('detectActiveSIPs') with 6 it.todo() stubs
    - allocation.test.ts: two describe blocks (AllocationTargetSchema, mapCategoryToAssetClass) with 8 it.todo() stubs
    - Both files: comment block showing future imports from @/lib/analytics/sip-detector and @/lib/analytics/asset-class-mapper
    - No actual imports from non-existent modules
    - Full npm test suite still passes
  </behavior>
  <action>
    Create tests/sip-detector.test.ts:
    ```typescript
    import { describe, it } from 'vitest'
    // Future import (created in Plan 02):
    // import { detectActiveSIPs } from '@/lib/analytics/sip-detector'

    describe('detectActiveSIPs', () => {
      it.todo('detects active SIP: 3+ same-amount transactions within 30-day cadence in last 90 days')
      it.todo('returns empty array when fewer than 3 recurring transactions')
      it.todo('returns empty array when transactions older than 90 days')
      it.todo('infers next debit date as lastDate + median interval (using date-fns addMonths)')
      it.todo('SIP XIRR uses only sip transaction_type cashflows')
      it.todo('SIP XIRR excludes purchase/lumpsum cashflows')
    })
    ```

    Create tests/allocation.test.ts:
    ```typescript
    import { describe, it } from 'vitest'
    // Future imports (created in Plan 02):
    // import { AllocationTargetSchema } from '@/lib/analytics/asset-class-mapper'
    // import { mapCategoryToAssetClass } from '@/lib/analytics/asset-class-mapper'

    describe('AllocationTargetSchema', () => {
      it.todo('accepts valid allocation summing to 100')
      it.todo('rejects allocation summing to more than 100')
      it.todo('accepts partial allocation summing to less than 100')
    })

    describe('mapCategoryToAssetClass', () => {
      it.todo('Equity Schemes → equity')
      it.todo('Debt Schemes → debt')
      it.todo('Gold ETF → gold')
      it.todo('International FoF → international')
      it.todo('unknown category falls back to equity')
    })
    ```

    After creating both files, run full suite to confirm 0 failures:
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>All 4 test scaffold files exist, all todo stubs pending, full npm test suite green (no regressions), npx tsc --noEmit clean.</done>
</task>

</tasks>

<verification>
- `npm test` passes with 0 failures (existing 37 tests pass + new stubs pending)
- 4 new test files present in tests/ directory
- Each file has a comment block showing future imports
- `npx tsc --noEmit` clean
</verification>

<success_criteria>
- tests/xirr.test.ts: 6 todo stubs under describe('computeXIRR')
- tests/analytics.test.ts: 8 todo stubs across 4 describe blocks
- tests/sip-detector.test.ts: 6 todo stubs under describe('detectActiveSIPs')
- tests/allocation.test.ts: 8 todo stubs across 2 describe blocks
- Full test run: 0 failures, 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-portfolio-analytics/02-01-SUMMARY.md`
</output>
