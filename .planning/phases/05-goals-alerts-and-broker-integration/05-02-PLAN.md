---
phase: 05-goals-alerts-and-broker-integration
plan: 02
type: tdd
wave: 2
depends_on: [05-01]
files_modified:
  - lib/analytics/goals-engine.ts
  - tests/goals-engine.test.ts
autonomous: true
requirements: [GOAL-01, GOAL-02, GOAL-03]

must_haves:
  truths:
    - "computeProjectedCorpus returns correct compound growth for any CAGR and year combination"
    - "computeGoalProjection returns isOnTrack=true when projected corpus >= target_amount"
    - "computeGoalProjection falls back to totalHolderAUM when currentLinkedValue is 0"
    - "progressPct is capped at 100 when currentLinkedValue exceeds target_amount"
    - "All 9 tests in goals-engine.test.ts are green (not todo)"
  artifacts:
    - path: "lib/analytics/goals-engine.ts"
      provides: "computeProjectedCorpus and computeGoalProjection — real implementation"
      exports: ["computeProjectedCorpus", "computeGoalProjection", "GoalProjection"]
    - path: "tests/goals-engine.test.ts"
      provides: "Full test coverage for goals projection engine"
  key_links:
    - from: "tests/goals-engine.test.ts"
      to: "lib/analytics/goals-engine.ts"
      via: "import { computeProjectedCorpus, computeGoalProjection }"
      pattern: "computeProjectedCorpus|computeGoalProjection"
---

<objective>
TDD implementation of the goals projection engine. Covers GOAL-01 (goal validation), GOAL-02 (linked holdings projection), and GOAL-03 (on-track/off-track status).

Purpose: This is pure business logic — testable arithmetic. TDD ensures the formula is correct before it reaches the UI.
Output: Fully tested goals-engine.ts with real implementations replacing the Wave 0 stubs.
</objective>

<execution_context>
@/Users/prasanthp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prasanthp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-goals-alerts-and-broker-integration/05-01-SUMMARY.md
@.planning/phases/05-goals-alerts-and-broker-integration/05-CONTEXT.md
@.planning/phases/05-goals-alerts-and-broker-integration/05-RESEARCH.md

<interfaces>
<!-- Locked formula from CONTEXT.md -->
Projected corpus: `current_linked_value × (1 + r/100)^years_to_target`
On-track: projected corpus >= target_amount
Off-track: projected corpus < target_amount
Fallback: when currentLinkedValue = 0, use totalHolderAUM as currentLinkedValue

date-fns `differenceInYears(futureDate, nowDate)` returns integer years.
For sub-year goals, use `differenceInCalendarDays` / 365 for fractional years.

Current stub interface (from Wave 0):
```typescript
export interface GoalProjection {
  projectedCorpus: number
  currentLinkedValue: number
  progressPct: number
  isOnTrack: boolean
  yearsToTarget: number
}

export function computeProjectedCorpus(
  currentLinkedValue: number,
  assumedCagrPct: number,
  yearsToTarget: number
): number

export function computeGoalProjection(
  goal: { target_amount: number; assumed_cagr: number; target_date: string },
  currentLinkedValue: number,
  totalHolderAUM?: number
): GoalProjection
```
</interfaces>
</context>

<feature>
  <name>Goals Projection Engine</name>
  <files>lib/analytics/goals-engine.ts, tests/goals-engine.test.ts</files>
  <behavior>
    - computeProjectedCorpus(100000, 12, 5) ≈ 176234 (±1)
    - computeProjectedCorpus(100000, 10, 0) = 100000 (no growth when yearsToTarget = 0)
    - computeProjectedCorpus(100000, 10, -1) = 100000 (no growth for past target dates)
    - computeGoalProjection with projected >= target: isOnTrack = true
    - computeGoalProjection with projected < target: isOnTrack = false
    - progressPct = min((currentLinkedValue / target_amount) * 100, 100)
    - When currentLinkedValue = 0 and totalHolderAUM provided: uses totalHolderAUM as base value
    - yearsToTarget uses fractional years (differenceInCalendarDays / 365)
  </behavior>
  <implementation>
    RED: Uncomment the import in tests/goals-engine.test.ts. Convert all it.todo() stubs to real test cases with assertions. Run tests — they MUST fail because goals-engine.ts still throws Not implemented. Commit: `test(05-02): add failing goals engine tests`

    GREEN: Replace stub implementations in lib/analytics/goals-engine.ts with real logic:
    - computeProjectedCorpus: if yearsToTarget <= 0, return currentLinkedValue; else return currentLinkedValue * Math.pow(1 + assumedCagrPct / 100, yearsToTarget)
    - computeGoalProjection: use differenceInCalendarDays from date-fns for fractional years; when currentLinkedValue = 0 and totalHolderAUM > 0, use totalHolderAUM as the base. Return full GoalProjection object.
    Run tests — all must pass. Commit: `feat(05-02): implement goals projection engine`

    REFACTOR (if needed): Add JSDoc to exported functions, extract constants. Run tests again. Commit: `refactor(05-02): clean up goals engine`
  </implementation>
</feature>

<verification>
`npx vitest run tests/goals-engine.test.ts` — 0 failures, 0 todos, all 9 tests green
`npx tsc --noEmit` — no type errors
</verification>

<success_criteria>
All goals-engine tests pass. computeProjectedCorpus(100000, 12, 5) returns value between 176230 and 176240. isOnTrack correctly reflects projected >= target comparison.
</success_criteria>

<output>
After completion, create `.planning/phases/05-goals-alerts-and-broker-integration/05-02-SUMMARY.md`
</output>
