---
phase: 04-ai-intelligence
plan: 06
type: execute
wave: 5
depends_on:
  - 04-03
  - 04-04
  - 04-05
files_modified: []
autonomous: false
requirements:
  - AI-01
  - AI-02
  - AI-03
  - AI-04

must_haves:
  truths:
    - "AI Portfolio Health card visible in holder page sidebar with quality score and alpha bars"
    - "Chat widget FAB visible on all dashboard pages"
    - "Chat responds with accurate portfolio data in natural language"
    - "Strategic Portfolio Narrative card on Tax & AI page with Generate Review button"
    - "SEBI disclaimers present in scorecard, narrative, and chat responses"
    - "Scores and narrative served from DB cache on page load"
  artifacts:
    - path: "components/ai/ai-portfolio-health.tsx"
      provides: "Rendered on holder page"
    - path: "components/ai/chat-widget.tsx"
      provides: "Floating FAB on all dashboard pages"
    - path: "components/ai/strategic-narrative.tsx"
      provides: "Rendered on Tax & AI page"
  key_links:
    - from: "holder page sidebar"
      to: "fund_ai_scores table"
      via: "Server Component fetch on page load"
      pattern: "fund_ai_scores"
    - from: "chat widget"
      to: "/api/ai/chat"
      via: "useChat streaming"
      pattern: "useChat.*chat"
    - from: "Tax & AI page"
      to: "portfolio_narratives table"
      via: "Server Component fetch on page load"
      pattern: "portfolio_narratives"
---

<objective>
Human verification checkpoint: Confirm all four AI features work end-to-end with a live Anthropic API key.

Purpose: AI features require live API calls and real portfolio data to verify — automated tests cannot confirm Claude's response quality, streaming behavior, or SEBI compliance of generated text.
Output: Phase 4 verified and signed off.
</objective>

<execution_context>
@/Users/prasanthp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prasanthp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-ai-intelligence/04-CONTEXT.md
@.planning/phases/04-ai-intelligence/04-03-SUMMARY.md
@.planning/phases/04-ai-intelligence/04-04-SUMMARY.md
@.planning/phases/04-ai-intelligence/04-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final TypeScript and test suite verification</name>
  <files></files>
  <action>
    Run the full automated verification before handing off for human review:

    1. Full TypeScript check: `npx tsc --noEmit`
    2. Full test suite: `npx vitest run`
    3. Verify all test files pass including tests/ai/scoring.test.ts and tests/ai/prompts.test.ts
    4. Verify no Lucide icon imports in any new AI components (only Material Symbols Outlined)
    5. Verify ANTHROPIC_API_KEY is only referenced in server-side files (lib/ai/, app/api/ai/) — never in components/ai/ except as props

    If any check fails, fix before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>cd /Users/prasanthp/Documents/Work/AIgency/FolioAI && npx tsc --noEmit && npx vitest run 2>&1 | tail -20</automated>
  </verify>
  <done>TypeScript clean; full test suite green; no client-side API key exposure</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of all AI features end-to-end</name>
  <action>No automated action — this is a human verification checkpoint. Follow the steps in how-to-verify below.</action>
  <what-built>
    Phase 4 AI Intelligence features:
    - AI-01: Fund scoring — POST /api/ai/score-funds computes quality scores + calls Claude for narrative prose
    - AI-02: Replacement recommendations embedded in quarterly narrative prose
    - AI-03: Floating chat widget on all dashboard pages — streams Claude responses with portfolio context
    - AI-04: Quarterly review narrative on Tax & AI page — on-demand generation, cached in DB
  </what-built>
  <how-to-verify>
    Prerequisites: Ensure ANTHROPIC_API_KEY is set in .env.local before testing.

    **Test 1 — Fund Scoring (AI-01):**
    1. Navigate to any Individual Holder page
    2. Scroll to the right sidebar — verify "AI Portfolio Health" dark card (bg-primary) is visible
    3. If no scores yet: note the card shows "No scores yet" message
    4. Open terminal and run: `curl -X POST http://localhost:3000/api/ai/score-funds -H "Content-Type: application/json" -d '{"holderId":"<your-holder-id>"}' -b cookies.txt`
       (Or click "Refresh Scores" button if it appears)
    5. Reload the holder page — verify circular quality score (0-100) appears with per-fund alpha bars
    6. Verify underperforming funds (negative alpha) show amber-colored bars
    7. Verify SEBI disclaimer text visible at card bottom

    **Test 2 — Quarterly Narrative + Recommendations (AI-04, AI-02):**
    1. Navigate to Tax Intelligence page (AI Insights nav also redirects here)
    2. Scroll to bottom — verify "Strategic Portfolio Narrative" full-width card appears
    3. Click "Generate Review" button — verify loading spinner appears
    4. Wait for response (~10-30 seconds) — verify narrative text appears with sections covering performance, exit candidates, sector concentration
    5. Verify "Generated X minutes ago" badge appears after generation
    6. Verify narrative mentions specific fund names with soft advisory language ("you may wish to consider...")
    7. Verify SEBI disclaimer footer: "FolioAI provides educational analysis. This is not SEBI-registered investment advice..."
    8. Click "Regenerate" — verify a new narrative is generated and replaces the old one

    **Test 3 — Chat Widget (AI-03):**
    1. Navigate to Family Dashboard
    2. Verify "Ask AI" pill/FAB visible in bottom-right corner
    3. Click FAB — verify 450px chat panel expands with "FolioAI Intelligence Hub" header
    4. Verify greeting message: "Hello! I can help with tax queries, sector exposure, or sell-impact analysis."
    5. Type "What is my total portfolio value?" — verify Claude responds with actual ₹ figure matching dashboard
    6. Type "Which of my funds has the highest expense ratio?" — verify Claude references actual fund names
    7. Type "How exposed am I to mid cap equity?" — verify Claude gives a percentage
    8. Navigate to Tax Intelligence page (keep chat open) — verify chat stays open (session persists)
    9. Verify SEBI disclaimer appears in Claude's responses when giving investment-related guidance
    10. Refresh the page — verify chat history clears (fresh session)

    **Test 4 — AI Insights Nav:**
    1. Click "AI Insights" in the left sidebar
    2. Verify it navigates to the Tax Intelligence page (same URL as Tax Intelligence)
    3. Verify the Strategic Portfolio Narrative card is visible on that page
  </how-to-verify>
  <resume-signal>Type "approved" if all four tests pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All four AI requirements verified:
- AI-01: Fund scoring with quality score + narrative
- AI-02: Replacement recommendations in narrative prose
- AI-03: Streaming chat with portfolio context
- AI-04: On-demand quarterly review narrative
</verification>

<success_criteria>
- All automated checks pass (TypeScript + Vitest)
- Human verifies AI Portfolio Health card renders with real scores
- Human verifies chat widget streams accurate responses
- Human verifies quarterly narrative generates with SEBI disclaimer
- Human confirms SEBI advisory language is appropriate (soft, non-directive)
</success_criteria>

<output>
After completion, create `.planning/phases/04-ai-intelligence/04-06-SUMMARY.md`
</output>
