---
phase: 01-data-foundation
task: discuss-phase
total_tasks: N/A (discussion session, not execution)
status: in_progress
last_updated: 2026-03-18T16:48:48.775Z
---

<current_state>
Mid-session of `/gsd:discuss-phase 1`. Three of four selected gray areas partially or fully discussed. CONTEXT.md has NOT been written yet — that happens at the end of the discussion. User selected all 4 areas to discuss: Tech Stack, CAS Parser Strategy, Data Model Design, NAV Data Source.
</current_state>

<completed_work>

- **Tech Stack** — FULLY DISCUSSED:
  - Frontend: Next.js
  - Backend: Next.js API routes (collocated, no separate server)
  - Auth + DB: Supabase (both)
  - Styling: Tailwind CSS + shadcn/ui

- **CAS Parser Strategy** — FULLY DISCUSSED:
  - Parsing approach: Hybrid — pdfplumber extracts raw text → Claude parses text-to-JSON with strict schema → programmatic validation before DB write
  - Where: Server-side in Next.js API route
  - Password-protected PDFs: Ask user to enter password at upload (PAN + DOB); decrypt server-side, never stored
  - On parse failure: Flag for manual review — import what can be parsed, flag unclear rows, user corrects manually

- **Data Model Design** — INTERRUPTED (questions were being asked, user paused before answering):
  - No answers captured yet for data model

- **NAV Data Source** — NOT DISCUSSED YET

</completed_work>

<remaining_work>

- **Data Model Design** — discuss 4 questions:
  1. Family/holder structure: `Family → Holders → Folios` (recommended) vs flat holders vs multi-family
  2. Transaction history granularity: Full ledger (recommended) vs snapshot vs partial
  3. Asset types in Phase 1: MF only (recommended) vs MF + stocks via manual entry
  4. Fund identity: AMFI scheme code as key (recommended) vs name vs folio number

- **NAV Data Source** — discuss 4 questions:
  1. Primary source: mfapi.in vs direct AMFI file scraping vs paid provider
  2. Fallback strategy: What happens if primary source is down
  3. Jan 31, 2018 NAV seeding: How to load this one-time data (needed for Phase 3 grandfathering — cannot be re-fetched later)
  4. Update frequency and timing: When does EOD sync run (cron time, retry logic)

- **Write CONTEXT.md** once all 4 areas discussed

</remaining_work>

<decisions_made>

- **Tech Stack**: Next.js + Next.js API routes + Supabase (auth + DB) + Tailwind CSS + shadcn/ui
- **CAS Parsing**: Hybrid — pdfplumber text extraction → Claude structured parsing → programmatic validation
- **CAS Parse location**: Server-side Next.js API route
- **CAS Password**: Ask user at upload time; decrypt server-side; never stored
- **CAS Failure**: Flag ambiguous rows for manual review; never silently drop; never fail entire import

</decisions_made>

<blockers>

- None. User paused voluntarily.

</blockers>

<context>
Running `/gsd:discuss-phase 1` for a greenfield Indian portfolio management app (FolioAI). This is the first phase — building the data foundation (schema, CAS import, NAV sync, holdings view). The tech stack and CAS parser decisions are locked. Data model and NAV source decisions still needed before CONTEXT.md can be written.

The project has Supabase MCP connected, which guided the Supabase decision.
CAS PDFs are CAMS/KFintech consolidated account statements — the primary way Indian mutual fund investors get their transaction history.
The Jan 31, 2018 NAV seeding is a critical blocker noted in STATE.md — needed for tax grandfathering in Phase 3.
</context>

<next_action>
Resume with: `/gsd:resume-work` — then continue discussing **Data Model Design** (4 questions), then **NAV Data Source** (4 questions), then write CONTEXT.md.
</next_action>
