---
phase: 06-tradebook-import
plan: "04"
type: execute
wave: 4
depends_on:
  - "06-03"
files_modified: []
autonomous: false
requirements:
  - DATA-03

must_haves:
  truths:
    - "User can upload a real (or sample) CSV tradebook and see the preview table"
    - "Valid rows are importable; invalid rows are highlighted red and skipped"
    - "After import, user is redirected to /families/[familyId]/holdings and can see imported stocks"
    - "Tradebook-imported stock rows show the upload_file badge and import date tooltip on symbol"
    - "Tradebook-imported stock rows show 'Read-only' in Actions column — no edit/delete buttons"
    - "Re-uploading the same file shows all rows as duplicates (0 new imports)"
    - "Zerodha-connected holdings are not corrupted after importing the same stock via tradebook"
  artifacts:
    - path: "app/(dashboard)/families/[familyId]/import/TradebookImportForm.tsx"
      provides: "Functional preview-then-confirm UI"
    - path: "app/api/holdings/import-tradebook/route.ts"
      provides: "Working deduplication and upsert"
    - path: "app/(dashboard)/families/[familyId]/holdings/page.tsx"
      provides: "Stock holdings view with tradebook attribution"
    - path: "components/holdings/stock-holdings-table.tsx"
      provides: "Table with tooltip and read-only enforcement"
  key_links:
    - from: "TradebookImportForm.tsx"
      to: "/api/holdings/import-tradebook"
      via: "fetch POST"
      pattern: "import-tradebook"
    - from: "TradebookImportForm.tsx"
      to: "/families/[familyId]/holdings"
      via: "router.push after import success"
      pattern: "router\\.push.*holdings"
---

<objective>
Human verification checkpoint for the complete tradebook import flow. Confirms the end-to-end workflow works correctly in a browser: file parsing, preview with validation highlighting, successful import, redirect to holdings page, tradebook attribution tooltip, read-only enforcement, deduplication on re-upload, and Zerodha data integrity.

Purpose: Automated tests cover the parsing library; this checkpoint verifies the UI interaction, the API route behaviour with real data, the DB state after import, and the holdings view correctly rendering tradebook-specific UI (tooltip, read-only).
Output: Phase 6 confirmed working.
</objective>

<execution_context>
@/Users/prasanthp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prasanthp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-i-want-to-add-option-to-import-stocks-bought-using-tradebooks-downloaded-manually/06-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify: End-to-end tradebook import flow</name>
  <action>Human verification — no automated action. Verify the complete tradebook import workflow in the browser using the steps in how-to-verify.</action>
  <what-built>
    Complete tradebook import feature:
    - Tradebook tab on /families/[familyId]/import page (third tab after CAS and Broker)
    - TradebookImportForm: holder dropdown, file picker (.csv/.xlsx), parse-preview-confirm workflow
    - Preview table with valid rows normal, invalid rows highlighted red
    - POST /api/holdings/import-tradebook: deduplicates by trade_id, upserts stock_transactions + stock_holdings
    - Post-import redirect to /families/[familyId]/holdings (stock holdings page)
    - Stock holdings page with tradebook attribution tooltip and read-only row enforcement
  </what-built>
  <how-to-verify>
    1. Navigate to /families/[your-family-id]/import — confirm 3 tabs: "CAS Import", "Broker", "Tradebook"
    2. Click Tradebook tab
    3. Confirm holder dropdown is visible with your family members listed
    4. Upload the sample CSV at tests/fixtures/sample-tradebook.csv:
       - Preview table should appear with 2 valid rows (INFY, RELIANCE) and 1 invalid row (red — missing ISIN)
       - Summary above table shows "2 valid rows • 1 will be skipped"
    5. Select a holder from the dropdown, then click "Import 2 transactions"
    6. Confirm success banner shows "Imported 2 stocks (0 duplicates skipped)"
    7. Confirm auto-redirect goes to /families/[familyId]/holdings (the stock holdings page — NOT the family dashboard)
    8. On the holdings page, confirm the two imported stocks (INFY, RELIANCE) appear in the table
    9. Hover over the INFY symbol cell — confirm tooltip shows "Imported via Tradebook • [date]"
    10. Confirm the upload_file icon badge appears next to the INFY and RELIANCE symbol text
    11. Confirm the Actions column for INFY and RELIANCE shows "Read-only" text (no edit/delete buttons)
    12. Navigate back to the Tradebook tab and upload the SAME file again
    13. Confirm "Import 2 transactions" button is available; click it
    14. Confirm success banner shows "Imported 0 stocks (2 duplicates skipped)"
    15. If you have a Zerodha-connected holder with INFY: confirm the stock holdings page shows source="Zerodha" for that row (not overwritten by tradebook), and that row DOES have edit/delete buttons (not read-only)
  </how-to-verify>
  <verify>
    <automated>npx vitest run tests/tradebook-parser.test.ts tests/tradebook-column-mapper.test.ts tests/tradebook-validator.test.ts tests/tradebook-mapper.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>All 15 browser verification steps pass. Automated tradebook tests pass. Phase 6 complete.</done>
  <resume-signal>Type "approved" if all checks pass, or describe which step failed</resume-signal>
</task>

</tasks>

<verification>
All steps in the how-to-verify checklist pass. Automated test suite still green.
</verification>

<success_criteria>
- Preview workflow works: valid rows shown normally, invalid rows red, count summary correct
- Import inserts into stock_transactions; stock_holdings reflects tradebook position
- Post-import redirect goes to /families/[familyId]/holdings (holdings page, not family dashboard)
- Holdings page shows tradebook rows with import date tooltip and upload_file badge
- Tradebook rows are read-only: no edit/delete buttons, "Read-only" label in Actions column
- Re-upload deduplication works (0 imported, N skipped)
- Zerodha holdings not overwritten by tradebook import of same symbol; Zerodha rows are NOT read-only
- Phase 6 complete
</success_criteria>

<output>
After completion, create `.planning/phases/06-i-want-to-add-option-to-import-stocks-bought-using-tradebooks-downloaded-manually/06-04-SUMMARY.md`
</output>
