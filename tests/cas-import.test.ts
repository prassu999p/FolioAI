import { describe, it } from 'vitest'
import { createMockSupabase } from './setup'

describe('CAMS PDF parse (DATA-01)', () => {
  it.todo('parse returns transactions with correct fields')
  it.todo('PAN matched to correct holder in family')
  it.todo('duplicate import deduplicates transactions')
})

describe('KFintech PDF parse (DATA-02)', () => {
  it.todo('parse returns transactions with correct fields')
  it.todo('PAN matched to correct holder in family')
  it.todo('duplicate import deduplicates transactions')
})
